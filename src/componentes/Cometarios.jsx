import { datosComentarios } from "../assets/comentarios"

export function Comertarios(){

    return(
        <div>
            {datosComentarios.map(
                
            )}
        </div>
    )
}